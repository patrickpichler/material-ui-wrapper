(ns material-ui-wrapper.core-test
  (:require [clojure.test :refer :all]
            [material-ui-wrapper.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (some? ((ns-publics 'material-ui-wrapper.core)) "Button"))))
